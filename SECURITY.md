# Security

Arab Writer is primarily an instruction-based skill.

The bundled scripts:
- read local text files supplied by the user;
- do not make network requests;
- do not execute user text;
- do not modify input files;
- do not require secrets.

Report security issues through a private channel if one is configured for the repository. Avoid publishing sensitive user text in public issues.
